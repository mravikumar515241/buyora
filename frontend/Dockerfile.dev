# Development frontend – runs Vite dev server
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
EXPOSE 3001
ENV VITE_API_BASE_URL=http://localhost:8081
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3001"]
